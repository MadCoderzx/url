{
  "name": "url-shortener-backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "db:init": "node src/db/setup.js",
    "test": "cross-env DATABASE_URL=postgres://urluser:urlpass@localhost:5433/urlshortener jest --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^7.2.2"
  }
}
